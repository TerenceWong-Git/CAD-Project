import { Checkbox } from "@mantine/core";
import { districts, types } from "../map/District";
import { JSXElementConstructor, Key, ReactElement, ReactFragment, useState } from "react";

export default function MultipleCategory(setValues: any) {
  // const [value, setValue] = useState<string[]>([]);
  // console.log(value);
  return (
    <>
      {/* {districts.map((district: any) => {
        return (
          <Checkbox.Group key={district} value={values} onChange={setValues}>
            <Checkbox value={district} label={district} />
          </Checkbox.Group>
        );
      })} */}
    </>
  );
}
